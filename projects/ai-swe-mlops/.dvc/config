[core]
    remote = local
['remote "local"']
    url = ../../../.dvc-store/ai-swe-mlops
# The remote URL is repo-relative and resolved against this config file.
# See docs/adr/0001-dvc-remote-strategy.md for the rationale.
