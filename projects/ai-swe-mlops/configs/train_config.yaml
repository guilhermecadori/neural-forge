experiment_name: classification

data:
  raw_path: data/raw/dataset.csv
  processed_dir: data/processed
  target_col: target
  test_size: 0.2
  random_state: 42

model:
  name: random_forest       # random_forest | logistic_regression
  params:
    n_estimators: 100
    max_depth: 10
    random_state: 42
    n_jobs: -1

artifacts:
  model_dir: models
